id: nsq.runtime.native.overhead.compensation.v1
type: stamp
version: 1
authority: Braxon
role: overhead_compensation_forms

meaning:
  reusable forms for prompt-route reuse, warm stamp replay, and court-path reuse so offline model overhead stays outside canonical runtime mass

forms:
  - compensate for repeated conversation overhead through reusable stamp bundles
  - keep full-off and full-on anchors intact while reusing graded lever positions
  - preserve canonical base-8 routing while minimizing runtime bulk

wake_key: oc
