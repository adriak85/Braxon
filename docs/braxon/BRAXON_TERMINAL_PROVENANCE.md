# Braxon Terminal Provenance

This file records the honest terminal/build boundary.

Braxon has absorbed Alien::Build, alienfile guidance, terminal behavior modules, pseudo-terminal references, color-depth behavior, completion width/height behavior, encoding behavior, FFI library discovery behavior, cargo, and rustc into NSQ foreign-tool absorption stamp bodies.

This means the terminal build has a local absorbed knowledge substrate.

It does not by itself prove hot-live autonomous execution.

## Correct use

Use this corpus to guide terminal wrapper construction, terminal repair, source-first build lanes, and Android/Termux build diagnosis.

## Boundary

Captured source plus translated NSQ body is provenance and guidance.

Hot-live execution requires wake dispatch, route execution, runtime proof, or an explicit verifier showing the terminal wrapper used the absorbed corpus successfully.

## Safety

Do not pull, reset, force-push, or discard phone state while transferring this terminal set.

Do not treat foreign runtime execution as allowed. The absorbed registry deliberately marks foreign runtime, parallel runtime, and silent foreign execution as false.
