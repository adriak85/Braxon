# NSQ Court Whole Architecture

NSQ Court is the architecture root.

Canonical court identity:

- king = compositor
- queen = linter
- court_is_agents = false

King and queen are canonical NSQ Court slots. They are not duplicate agents and must not be rewritten into generic vocabulary.

Downloader, semantic benchmark, NSQASM compilation, model acquisition, bare-tasker emission, and perpetual-runtime proof surfaces are court-owned routes. They are not independent sibling systems.

C reference runner acceptance is forbidden. C reference runner storage is forbidden. Do not quarantine it. Do not preserve it as fallback. Remove it.

Accepted path:

NSQ task/spec -> NSQ Court route -> NSQASM/AArch64 -> native host smoke artifact and/or flat bare-form artifact.
