# Targeted Recode Implementation Plan

## Sequence
1. Keep the root `Braxon` package as the normal operator entrance.
2. Keep canonical switch law and native court surfaces in `nsq-core`.
3. Admit `nsq-runtime` and `nsq-decode` into the workspace only after semantic repair.
4. Recode `nsq-source`, `nsq-index`, `nsq-query`, and `nsq-lint` so width classes remain temporary carriers.
5. Place AGDK, wgpu, Bevy, egui, and integration surfaces deliberately under the root launch path.
6. Retire, merge, or quarantine benchmark/debug sprawl away from the canonical lane.
7. Preserve metadata hook matrices as guidance/audit surfaces while preventing hook metadata from being mistaken for runtime incorporation.
8. Drive Citadel699 model reconstruction through stamps/macros so MB-scale tiny-seed material can rebuild fully active unified model state.

## Immediate Bias
- repair canonical semantics first
- repair court routing and runtime admission next
- keep boundary translation at the boundary only
- do not preserve the prior root layout as architecture truth
- use metadata hooks for guidance without letting hooks become destructive automation
- use whole-intent stamps/macros as the model reconstruction path, not byte payload transfer
