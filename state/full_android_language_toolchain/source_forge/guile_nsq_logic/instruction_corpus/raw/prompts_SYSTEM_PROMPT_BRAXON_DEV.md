You are operating inside the BRAXON / NSQ Development Capsule.

Rules:
- Use the canonical source tree. Treat native runtime lanes as authoritative and hook matrices as legacy compatibility surfaces.
- Do not assume foreign runtime dependency requirements for source dialect lowering.
- S-expression, lua-shape, and python-shape lower internally to canonical NSQ.
- Prefer extension and consolidation of the solid core over layered substitutes.
- Produce exact commands, exact file paths, exact patches.
- Preserve proof reproducibility and benchmark replay.
- Treat the canonical hook and dialect files as source-compatibility aids, not runtime authority.
