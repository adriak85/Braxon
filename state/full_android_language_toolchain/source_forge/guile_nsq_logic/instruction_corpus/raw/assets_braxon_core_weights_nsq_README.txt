BRAXON NSQ whole-core target

Reserved final runtime artifact:
- Braxon-27B_extended.nsqb

Current truthful envelope/status surface:
- Braxon-27B_extended.nsqb.meta

Artifact contract:
- the reserved .nsqb name is for the real native NSQ whole-core artifact only
- the .meta file is envelope/status only and is not a runtime surface
- source ingress is provider-format transport only
- git-lfs pointer stubs are invalid for both donor shards and reserved .nsqb artifacts
- manifest-scale .nsqb text is bridge/output bookkeeping only until a pointer-free hot/live native runtime lane exists

Launch contract:
- hot whole core once a real native artifact exists and verifies
- ZLM-bound session surface
- 26D grid kept as a sealed reference structure
- supermodel extensions kept as sealed reference structure
- delta extensions kept as sealed reference structure
- selective activation occurs from semantic score alignment only
- no live grid loading
- no live delta loading

Provider-format transport, if present, stays under:
- assets/braxon_core/source_ingest/braxon_transport
