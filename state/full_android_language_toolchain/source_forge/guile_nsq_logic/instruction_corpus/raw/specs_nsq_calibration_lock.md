# NSQ Calibration Lock v0

A calibration lock is a deterministic corpus-derived control surface.

It may lock:
- selected optimization profile
- promoted auto macros
- boundary carrier projections
- rebalance recommendations

The compiler may consume this lock, but must not mutate it.
