#!/data/data/com.termux/files/usr/bin/bash
set -euo pipefail
ROOT="${BRAXON_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}"
exec python3 "$ROOT/tools/BRAXON_model_downloader/BRAXON_model_downloader_app.py" "$@"
