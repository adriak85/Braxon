#!/data/data/com.termux/files/usr/bin/bash
set -euo pipefail
ROOT="${BRAXON_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}"
exec python3 "$ROOT/tools/nsq_court_identity_guard/check_court_identity.py" "$ROOT"
