#!/data/data/com.termux/files/usr/bin/bash
set -euo pipefail
. "$(cd "$(dirname "$0")/.." && pwd)/env/BRAXON_env.sh"

cd "$BRAXON_HOME"

cargo build -q --release \
  -p nsq-compile \
  -p nsq-native-bench \
  -p nsq-real-bench \
  -p nsq-cli

echo "build complete"
