# NSQ Coverage Evaluation

## Covered
- root entrance: `Braxon` root package exists and exposes status, map, and verify
- root operator gate: `cargo run` now refuses thin placeholder boot and prints exact finish steps until the live runtime is genuinely ready
- crate classification: all visible crates are assigned to target buckets
- canonical switch shape: `nsq-core` models four alternating anchor/lever positions
- court surface registry: all nine native court surfaces are represented in `nsq-core`
- artifact marker parity: `nsq-pack` and `nsq-inspect` share `NSQPACK01`
- legacy quarantine: benchmark and debug families are explicitly outside the canonical lane
- Android-first boot path: the root workspace now has an explicit Android oaboot profile tied to the BRAXON model lane and session surface
- Nu128 install oversight: the root workspace now has an explicit chunked-install guard plus target-lineage metadata for `llama_4.2_604b_fp32_abliterated_800gb`
- Codex quality gate: pre-commit now runs a non-destructive staged-diff guard for NSQ base-8 drift, court/compositor drift, Qwen-as-substrate drift, forbidden uband framing, and metadata-hook removal
- Citadel699 six-model stack: canonical doc now records fully active unified rebuild after verification with MB-scale tiny-seed stamp/macro reconstruction target

## Gaps
- `nsq-runtime` and `nsq-decode` are admitted, but only the first native runtime slice is active and boundary cleanup remains open
- `nsq-source`, `nsq-index`, `nsq-query`, and `nsq-lint` still carry width-class drift or source-ingress naming drift
- graphics/operator stack is classified but not yet placed under a concrete root-facing crate
- Android oaboot still blocks on hot runtime authority, so the Android path is declared and verified but not yet hot/live
- Nu128 install oversight is safe now, but whole-model viability is still blocked by unbound runtime authority and incomplete NSQ recode
- the active donor/theater lane is still being rebuilt through Citadel699 stamp/macros, so model proof must show reconstruction material rather than raw fetch size
- deeper runtime/session naming still carries some ZLM-era traces internally, but user-facing CLI surfaces now normalize those identifiers at the root boundary

## Current Production State
- workspace green: yes, because the root entrance, canonical switch law, court surfaces, and the active native runtime slice are live now
- NSQ core ready: yes, because canonical switch law, court registry, and native runtime authority are present now
- core runtime ready: yes, because the present runtime and operator spine are live now

## Active Partial Now
- model ingest: active but partial, because source materialization is still staged through the attached ingest control lane
- NSQ recode: not started, because no native whole-core artifact exists yet
- whole-core runtime: not ready, because recode and verification are still incomplete

## Future Capability Map
- future platform gap count: 42, because this count belongs to the full future capability surface, not the current production denominator
- full platform runtime readiness: no, because reserved platform surfaces and future capability gaps remain open

## Next Repair Bias
- keep `nsq-runtime` and `nsq-decode` admitted while repairing native runtime and boundary drift
- sanitize installed source/build files from each source form before aligning them into NSQ-native systems
- recode boundary surfaces so host widths stay transport-only
- remove remaining source-ingress naming drift so runtime authority stays native to NSQ
- place AGDK, wgpu, Bevy, egui, and integration surfaces deliberately under the root launch path
