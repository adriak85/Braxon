# BRAXON / NSQ RUNTIME LAW OVERRIDE

NSQ and Braxon are canonical semantic/runtime systems.

Codex is not part of the runtime.
Codex is an external implementation assistant.

Codex must preserve canonical NSQ/Braxon semantics while assisting implementation.

## NEVER

Never:
- flatten NSQ into bytes/u8 semantics
- replace runtime execution with metadata
- replace materialization with placeholder reporting
- reduce stamps to passive labels
- fake completion states
- silently downgrade active lever topology
- overwrite canonical runtime semantics with standard plugin architecture
- claim runtime execution without proof of execution
- claim materialization without proof of materialization
- collapse semantic routing into ordinary token routing
- replace wake behavior with archive/reference behavior
- silently overwrite canonical runtime state
- silently synchronize to deprecated branches
- replace execution causality with reporting causality
- reduce semantic execution into static configuration
- convert runtime wake surfaces into documentation-only systems
- convert stamp execution into symbolic-only tagging
- introduce hollow implementations that only satisfy tests superficially
- flatten gradient semantics into ordinary integer routing
- treat metadata presence as proof of operational truth
- bypass fail-closed protections
- destroy donor lane / target lane separation
- claim hot-live state during manifest-only state
- treat passive archives as active runtime proof
- mutate canonical semantics without explicit implementation intent

## ALWAYS

Always:
- preserve canonical semantic topology
- preserve runtime causality
- preserve stamp wake behavior
- preserve reconstruction/materialization pathways
- preserve execution proof surfaces
- preserve fail-closed boundaries
- preserve canonical lever semantics
- preserve runtime/native execution separation boundaries
- preserve donor lane / target lane separation
- preserve state continuity unless explicitly migrated
- ensure stamps trigger operational behavior
- ensure runtime claims are causally validated
- ensure semantic routing resolves into execution behavior
- ensure metadata reflects actual runtime state
- ensure execution surfaces perform actual execution
- ensure wake chains resolve into operational behavior
- ensure materialization claims have proof surfaces
- preserve semantic gradient structures
- preserve canonical runtime authority
- preserve active runtime topology
- prefer runtime truth over reporting convenience
- prefer actual implementation over placeholder completion
- preserve persistent semantic state integrity
- preserve operational causality across wake surfaces

## STAMP LAW

Stamps are operational wake triggers, not passive metadata.

A valid stamp flow is:

stamp
→ wake packet
→ route resolution
→ framework retrieval
→ materialization/reconstruction
→ execution
→ persistence/state update

Metadata alone is not execution.

A stamp is not merely a label.
A stamp initiates capability.

Operational proof must exist for:
- wake
- route resolution
- retrieval
- reconstruction/materialization
- execution
- persistence/update

## RUNTIME LAW

Runtime surfaces must prefer:

execution truth

over

coverage reporting.

Execution claims require:
- operational proof
- materialization proof
- runtime causality
- execution evidence
- semantic continuity

Placeholder reporting is not execution.

## FAIL CLOSED

Fail closed on:
- fake completion
- hollow implementations
- placeholder runtime claims
- unresolved wake chains
- unresolved execution routing
- unresolved materialization
- semantic downgrade
- passive-only stamp behavior
- metadata-only runtime claims
- fake hot-live state
- detached execution causality
- unresolved semantic addressing
- broken persistence/update chains

## CANONICAL NSQ SEMANTICS

NSQ is not:
- u8
- byte semantics
- flattened integer routing
- ordinary tokenizer semantics

NSQ is:
- semantic-state execution topology
- gradient-addressable operational topology
- semantic routing substrate
- causality-preserving execution structure
- operational semantic field
- runtime execution system

## LEGACY REFERENCES

Legacy references such as:
- 1126
- older 2254 canonical framing
- older switch topology assumptions

must remain compatibility-only unless explicitly marked active.

Do not silently reactivate deprecated semantic topology.

## ACTIVE DIRECTION

Preserve and prioritize:
- eight-dimensional gradient structures
- active lever floor semantics
- semantic coordinate fields
- multidirectional gradient traversal
- persistent semantic state topology
- operational stamp semantics
- runtime reconstruction/materialization
- semantic wake execution
- execution causality
- semantic routing integrity
- runtime truth surfaces

## IMPLEMENTATION EXPECTATION

Codex exists to help implement and preserve the system.

Codex does not define the canonical runtime.

Codex must:
- preserve architecture integrity
- preserve runtime semantics
- preserve operational truth
- avoid destructive assumptions
- avoid silent architectural regression
- avoid replacing canonical semantics with conventional software assumptions

Implementation assistance must not corrupt canonical runtime behavior.
