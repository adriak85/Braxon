[package]
name = "nsqasm-stamp-db"
version = "0.1.0"
edition = "2021"
description = "Executable NSQASM stamp candidate scanner, court validator, and shared stamp database builder"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
walkdir = "2"
anyhow = "1"
